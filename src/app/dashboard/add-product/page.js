import React from 'react';

export const metadata = {
    title: "Next Try || dashboard -- add product",
    description: "description",
  };

const AddProductPage = () => {
    return (
        <div>
            add product
        </div>
    );
};

export default AddProductPage;