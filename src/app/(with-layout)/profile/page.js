import React from 'react';

export const metadata = {
    title: "Next Try || profile",
    description: "description",
  };

const ProfilePage = () => {
    return (
        <div>
            profile
        </div>
    );
};

export default ProfilePage;