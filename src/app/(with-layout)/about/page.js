import React from 'react';

export const metadata = {
    title: "Next Try || About",
    description: "description",
  };

const AboutPage = () => {
    return (
        <div>
            about page
        </div>
    );
};

export default AboutPage;